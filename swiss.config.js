export default {
	theme: {
		//  The only color allowed.
		accentColor: '#f6be1f',

		// ? Mode: 'brand, 'negative' or 'positive'
		mode: 'accent',

		// Font familty
		typography: {
			// ? User can overright default font here
			fontFamily: 'arial'
		},
	}
}
