export default {
	theme: {
		//  The only color allowed.
		accentColor: '#43aa53',

		// Mode: 'brand, 'negative' or 'positive'
		mode: 'positive',
		// mode: 'negative',
		// mode: 'brand',

		// Font familty
		typography: {
			fontFamily: {
				isHelvetica: true,
			}
		},
	}
}
